{
  "name": "@database-craft/prisma-crud",
  "version": "1.0.0",
  "description": "Prisma CRUD operations with User/Post models, relations, pagination, filtering, and transactions",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "seed": "ts-node seed.ts",
    "demo": "ts-node operations.ts",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
