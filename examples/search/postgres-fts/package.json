{
  "name": "@database-craft/postgres-fts",
  "version": "1.0.0",
  "description": "Postgres full-text search — tsvector column, GIN index, to_tsvector/plainto_tsquery/ts_rank",
  "private": true,
  "scripts": {
    "migrate": "psql $DATABASE_URL -f migration.sql",
    "demo": "ts-node search.service.ts"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
