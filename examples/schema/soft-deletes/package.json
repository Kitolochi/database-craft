{
  "name": "@database-craft/soft-deletes",
  "version": "1.0.0",
  "description": "Soft delete pattern with Prisma middleware — query scoping, restore, purge with retention period",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "demo": "ts-node operations.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
